{
  "hooks": {
    "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
    "pre-commit": "lint-staged",
    "pre-push": "npm run lint && npm run test:ci && npm run build-storybook && npm run build"
  }
}
